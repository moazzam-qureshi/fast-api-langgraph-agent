# Production Environment Variables for Railway

# OpenAI API Key for embeddings and LLM (REQUIRED)
OPENAI_API_KEY=your-openai-api-key

# JWT Configuration (REQUIRED - generate with: openssl rand -hex 32)
JWT_SECRET_KEY=your-very-long-random-secret-key-change-this
JWT_ALGORITHM=HS256
JWT_EXPIRATION_DELTA=30

# Database (Railway will auto-inject these when using Railway Postgres)
# POSTGRES_PASSWORD=auto-generated-by-railway
# DATABASE_URL=auto-generated-by-railway

# Redis (Railway will auto-inject when using Railway Redis)
# REDIS_URL=auto-generated-by-railway

# MinIO Object Storage (REQUIRED for document storage)
MINIO_ROOT_USER=your-minio-admin-user
MINIO_ROOT_PASSWORD=your-secure-minio-password
MINIO_ENDPOINT=minio:9000
MINIO_SECURE=false

# LangChain Tracing (OPTIONAL)
LANGCHAIN_API_KEY=your-langchain-api-key
LANGCHAIN_TRACING_V2=true
LANGCHAIN_PROJECT=fastapi-langgraph

# Application Port (Railway will auto-inject PORT)
# PORT=8000 QureshiDesktopfastapi.env.example
